/**
 * The start page: the explorer with nothing selected. The map, the lists and
 * the season bar are the layout's; this page only fills the detail slot,
 * and with no entity in the path there is nothing to fill it with.
 */
const Page = () => null;

export default Page;
