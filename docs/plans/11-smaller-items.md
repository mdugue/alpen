# 11 · Smaller items

**Status:** [in progress](https://github.com/mdugue/alpen/pull/16) · **Effort:** S each · **Depends on:** noted per item

Independent clean-ups. Each is a PR on its own or rides along with the plan
that touches the same file. Tick them off here.

| #   | Item                                                                                                            | Where                                                                                                                                                            | Effort | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Favicon and app icons                                                                                           | `app/icon.svg`, `app/apple-icon.png`, `app/manifest.ts`                                                                                                          | S      | Every page load currently logs a 404 for `favicon.ico`; the tab shows no icon. Use the pass-circle glyph in the accent colour; manifest with both theme colours and `display: standalone`.                                                                                                                                                                                                                                                                             |
| 2   | `robots.ts` and `sitemap.ts`                                                                                    | `app/`                                                                                                                                                           | S      | Trivial now; lists entity routes after plan 02.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | Explorer context instead of prop drilling — [done](https://github.com/mdugue/alpen/pull/62)                     | `components/explorer.tsx`, `lib/app-state.ts`                                                                                                                    | M      | Was: `Sidebar` took ~20 props and an `ExplorerProvider` was to carry them. Closed by plan 28: the rule is one `reduce`, and its transport into the tree is the single `dispatch` prop, which React Compiler makes as cheap as a context. No `lib/explorer-context.tsx` exists.                                                                                                                                                                                         |
| 4   | Client fetch cache                                                                                              | `lib/use-fetch.ts`                                                                                                                                               | S      | Switching between two passes refetches the weather. A module-level `Map<url, Promise>` + React `use()` fixes it; obsolete after plan 02 (server-rendered weather), so only do it if 02 is far away.                                                                                                                                                                                                                                                                    |
| 5   | Redundant `"use cache"` in `lib/data.ts`                                                                        | `lib/data.ts`                                                                                                                                                    | S      | The page-level cache makes the function-level ones redundant; make them plain functions and verify the build log still shows `○`. Plan 09 replaces them with parsed exports anyway.                                                                                                                                                                                                                                                                                    |
| 6   | Slug index instead of `find`                                                                                    | `lib/status.ts`, `lib/rows.ts`, `components/panel/detail-panel.tsx`                                                                                              | S      | `tourStatus` calls `passes.find` per slug; build `indexBySlug(passes)` once in the explorer and pass the map down. Also makes tests simpler. `indexBySlug` exists in the explorer now; the panel still rebuilds it, which plan 17 closes.                                                                                                                                                                                                                              |
| 7   | Precomputed neighbours                                                                                          | `lib/nearby.ts`, `lib/data.ts`                                                                                                                                   | S      | Done for tours in plan 01: which tours run within reach of each entity, and how near their road comes, is computed on the server (`getNearbyTours`), so no geometry reaches the client. Passes and towns are still measured in the panel – points, a hundred haversines, all through `lib/reach.ts` since plan 31. Only worth a file in `data/generated/` if more than the distance is wanted.                                                                         |
| 8   | Structured tour season                                                                                          | `data/tours.json`, `lib/types.ts`, `components/sidebar/tour-list.tsx`                                                                                            | S      | `season` is free text and the row does `split(";")[0]`. Use `{ opens, closes, note }` like passes; `check-data` validates; the strip in plan 03 then works for tours from data instead of from the worst pass only.                                                                                                                                                                                                                                                    |
| 9   | `window.innerHeight` in render — [done](https://github.com/mdugue/alpen/pull/16)                                | `components/explorer.tsx`                                                                                                                                        | S      | Was: `insetBottom` read `window.innerHeight` during render, not reactive and only harmless because `isMobile` is false on the server. Now `useViewportHeight` in `lib/use-media-query.ts`, a `useSyncExternalStore` hook, and `snapPx` in `components/mobile-sheet.tsx` turns the sheet's snap point into that padding.                                                                                                                                                |
| 10  | `useStored` initial values — [done](https://github.com/mdugue/alpen/pull/62)                                    | `lib/use-stored.ts`                                                                                                                                              | S      | Was: `useStored("alpenpaesse:overlays", ["hillshade"])` passed a fresh array each render and only worked because of the cache. Now `useStored(key)` takes a key of the `STORAGE` table and nothing else; every default is a module constant there, which the table's doc comment says.                                                                                                                                                                                 |
| 11  | Weather route hygiene                                                                                           | `app/api/weather/[slug]/route.ts`                                                                                                                                | S      | Validate `slug` against the slug regex before the lookup and send `Cache-Control: s-maxage=1800, stale-while-revalidate=3600` so the CDN serves repeats; the response is currently `must-revalidate`. Disappears with plan 02.                                                                                                                                                                                                                                         |
| 12  | Global Escape                                                                                                   | `components/panel/detail-panel.tsx`                                                                                                                              | S      | Escape only closes the detail when focus is inside it. A `keydown` listener on `document` while a selection exists (ignoring open dialogs and inputs) matches user expectation.                                                                                                                                                                                                                                                                                        |
| 13  | Promote "Route fehlt" to an error — folded in ([#62](https://github.com/mdugue/alpen/pull/62))                  | `scripts/check-data.ts`                                                                                                                                          | S      | Folded by plan 32 phase B rather than promoted: `data:check` now reads the build's own verdict for the key. A missing route is either one the next `data:build` fetches (a warning that says so), a rejection (reported with its measured values) or a ride behind a marker the build refuses to route to (reported with that marker). None of those is a regression an error would catch, so no case is left for one.                                                 |
| 14  | One palette module                                                                                              | `lib/palette.ts`                                                                                                                                                 | S      | `app/opengraph-image.tsx` keeps sRGB copies of the tokens; plan 06 needs the same list for the map style. One module, one comment saying "mirror of `app/globals.css`", and a `check` script that compares it with the CSS file.                                                                                                                                                                                                                                       |
| 15  | Check `data:build --status` in CI                                                                               | `.github/workflows/ci.yml`                                                                                                                                       | S      | Print the backlog in every CI run so a stalled pipeline is visible on PRs, not only in the refresh workflow's log.                                                                                                                                                                                                                                                                                                                                                     |
| 16  | Popover for the hover popup on touch — [done](https://github.com/mdugue/alpen/pull/62)                          | `lib/map-scene.ts`                                                                                                                                               | S      | Nothing to build: a tap selects, and a label under the finger would cover what was just tapped. Written down where the decision now lives – `SceneInput.env.coarse` in `lib/map-scene.ts`, where a coarse pointer makes the scene's `popup` `null`.                                                                                                                                                                                                                    |
| 17  | How each data file is read and written — [done](https://github.com/mdugue/alpen/pull/62)                        | `lib/schema.ts`, `scripts/build-data.ts`, `scripts/check-data.ts`, `scripts/locate-pass.ts`, `scripts/build-photos.ts`                                           | M      | `FILES` now carries, per file, the schema, where it lives, whether it may be missing and how it is laid out; `readData`/`writeData` in `scripts/lib/data-files.ts` are the only pair that touches `data/`. The six readers and three writers are gone, `Generated` in `build-data.ts` with them, and a reader hands back what the file says rather than the copy zod rebuilt in the schema's key order.                                                                |
| 18  | Thresholds: one source, prose checked                                                                           | `scripts/lib/validate.ts`, `lib/status.ts`, `docs/scales.md`, `docs/data-model.md`, `.agents/skills/curate-data/SKILL.md`, `docs/plans/00-route-quality-gate.md` | S      | `LIMITS` is restated as a table in three documents and the status constants in `docs/scales.md`; nothing fails when they drift, unlike `data/schema/*.json`. A check in `data:check` that every constant's value appears in the documents that quote it, the way the schema staleness check works. The documents stay hand-written on purpose.                                                                                                                         |
| 19  | Climate bucketing out of the fetcher — [done](https://github.com/mdugue/alpen/pull/62)                          | `scripts/build-data.ts:517-549`, `scripts/lib/`                                                                                                                  | S      | `bucketClimate` is a pure function in `scripts/lib/climate.ts` with a fixture test over a handful of synthetic days; the fetcher measures, it judges. It lives beside the other script-side rules rather than next to `profileStats`, which is client code. Storing the raw daily series so a bucketing change costs no API calls is still open.                                                                                                                       |
| 20  | Hover popup content from one lookup — [done](https://github.com/mdugue/alpen/pull/62)                           | `lib/map-scene.ts`, `components/map/apply-scene.ts`, `lib/map-assets.ts`                                                                                         | S      | Closed by plan 30 step 4: the label is `PopupContent` – a typed value the scene looks up from the hovered entity, tags as `Tag[]` and not a comma string – and `popupHtml` behind the applier renders it. Nothing is read off a rendered feature any more, so `subtitle` left the route and tour GeoJSON with it.                                                                                                                                                      |
| 21  | `analyze-status.ts` reuses the judge                                                                            | `scripts/analyze-status.ts`, `lib/status.ts`                                                                                                                     | S      | `winterOnly`, `bestRunLength` and a literal `snow >= 20` re-implement rules the script imports the module for, then it reconciles against `bestPeriods` at run time. Export `longestRun`, read `SIGNALS` (plan 16), drop the self-check. Do it when `bestPeriods` next changes.                                                                                                                                                                                        |
| 22  | Weather route parses its answer                                                                                 | `app/api/weather/[slug]/route.ts`                                                                                                                                | S      | `WeatherDay` has a zod schema and the route casts around it (`as number`), then the client casts again. One `parse`, as `lib/schema.ts` promises. Disappears with plan 02.                                                                                                                                                                                                                                                                                             |
| 23  | Mirrored numbers, TS ↔ CSS                                                                                      | `components/explorer.tsx`, `app/globals.css`, `lib/use-media-query.ts`                                                                                           | S      | `SIDEBAR_W`/`DETAIL_W` against `w-96 xl:w-104`, `LIST_PEEK` against `--sheet-peek`, `GAP` against `+ 12` in the map, `MOBILE_QUERY` against `max-lg`. CSS cannot read TS, so not a module: extend item 14's check script to these pairs.                                                                                                                                                                                                                               |
| 24  | The half-month resolved before the first paint — [done](https://github.com/mdugue/alpen/pull/62)                | `lib/app-state.ts` (`load`), `lib/hash-adapter.ts`                                                                                                               | S      | Was: the stored half-month was applied only in the hash effect, so a returning visitor saw the season band, the headline and the lists flip and 258 entities were graded twice before the first interaction. Now the precedence (link, then the visitor's choice, then today) is one line in `load`, dispatched once from a layout effect after hydration, so nothing flips after the hydrated paint.                                                                  |
| 25  | `back` clears `hovered` — [done](https://github.com/mdugue/alpen/pull/62)                                       | `lib/app-state.ts` (`close`)                                                                                                                                     | S      | Was: Escape left the last hovered entity ringed on the map and tinted in its row. Now the `back` case of `reduce` clears the hover and the profile cursor with the selection, and a `load` without an entity goes through the same `close`.                                                                                                                                                                                                                            |
| 26  | `statusOf` is dead and its body inlined — done (plan 31 phase B)                                                | `lib/status.ts`, `lib/destination.ts`                                                                                                                            | S      | Was: zero references anywhere, while the same expression was written by hand in `cellOf`. Now `cellOf` imports it, and `GRADE_ORDER` comes from `lib/status.ts` too instead of a second copy.                                                                                                                                                                                                                                                                          |
| 27  | `townReach` docstring — [done](https://github.com/mdugue/alpen/pull/62)                                         | `components/map/pass-map.tsx`                                                                                                                                    | S      | The prop now says what plan 30 step 3 made true: the hull is drawn while a town is hovered, in the list or on the map, and a selected town is flown to instead – from inside the hull there is nothing to see.                                                                                                                                                                                                                                                         |
| 28  | `--explain` asserts a full verdict from partly `null` metrics — [done](https://github.com/mdugue/alpen/pull/62) | `scripts/check-data.ts:428`, `scripts/lib/validate.ts:179-187`                                                                                                   | S      | `limitsJudged` (`scripts/lib/validate.ts`) says which limits the stored metrics could be weighed against, and "würde mit den heutigen Grenzen bestehen" now names them and the ones no profile was there to judge. No rejection currently sits in that branch, so the output is unchanged today.                                                                                                                                                                       |
| 29  | `"test": "bun test lib"` matches by path substring — done (plan 32 phase A)                                     | `package.json:15`                                                                                                                                                | S      | Was: it ran `scripts/lib/*.test.ts` by accident; a test at `scripts/pipeline.test.ts` would silently not run. Now `bun test lib scripts`, and CI runs `bun run test`.                                                                                                                                                                                                                                                                                                  |
| 30  | `--only` is honoured by 2 of 9 counters — [done](https://github.com/mdugue/alpen/pull/62)                       | `scripts/build-data.ts:127-129, 753, 766, 775-793`                                                                                                               | S      | `--only` is applied once, to the job list `plan` is made from, so every job kind and every counter honours it – routes, profiles, climate series, summits, road distances, and the rejected/stale/upgradable/kept numbers of the report.                                                                                                                                                                                                                               |
| 31  | Two spent migrations — [done](https://github.com/mdugue/alpen/pull/62)                                          | `scripts/build-data.ts:843-875, 895-946`                                                                                                                         | S      | `reconcileInputs` and `--backfill` are deleted; `inputs` is required in both schemas and `data/schema/*.schema.json` regenerated, after `data:check` confirmed that all 406 meta entries and all 9 rejections carry one.                                                                                                                                                                                                                                               |
| 32  | `PhotoCarousel` documents props it no longer has — done (plan 31 phase A)                                       | `components/panel/photo-carousel.tsx`                                                                                                                            | S      | Was: the comment explained `count` and `fallback` while the interface had been `{ loading, photos, onBroken }` since PR #49, with `const shown = photos` as the residue. The prose now describes the props it has, and the alias is gone.                                                                                                                                                                                                                              |
| 33  | The refresh workflow leaves the status snapshot stale                                                           | `.github/workflows/refresh-data.yml`, `lib/status.test.ts:715`, `lib/__snapshots__/status.test.ts.snap`                                                          | S      | `chore(data): refresh precomputed data` pushes climate series and profiles that change the matrix over all passes × 24 half-months, and a push by the workflow's token triggers no CI run, so `main` is red without a run saying so (seen at `0ef82da`: Bocca del Creer's series and Punta Veleno's profile). Either the workflow runs `bun test lib --update-snapshots` and commits the snapshot with the data, or the refresh PR is opened as a PR so CI runs on it. |

## Acceptance criteria

Each item states its own; the shared ones are the pre-PR checks in
`AGENTS.md` and a screenshot for anything visible.
