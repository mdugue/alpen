{
  "$id": "towns.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "aliases": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 2
        }
      },
      "country": {
        "type": "string",
        "enum": [
          "FR",
          "IT",
          "CH",
          "AT",
          "DE",
          "SI"
        ]
      },
      "lat": {
        "type": "number",
        "minimum": 43,
        "maximum": 49
      },
      "lon": {
        "type": "number",
        "minimum": 4,
        "maximum": 16
      },
      "name": {
        "type": "string",
        "minLength": 2
      },
      "slug": {
        "type": "string",
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
      },
      "tags": {
        "minItems": 1,
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "hub",
            "passes",
            "workshops",
            "hotels",
            "scenic",
            "quiet",
            "train",
            "events",
            "season"
          ]
        }
      },
      "why": {
        "type": "string"
      }
    },
    "required": [
      "country",
      "lat",
      "lon",
      "name",
      "slug",
      "tags",
      "why"
    ],
    "additionalProperties": false
  }
}
